---
name: codeQL-task
on:
  push:
    branches:
      - "develop"
      - "main"

jobs:
  codeql-job:
    name: codeql-job
    steps:
      - name: Run codeQL-task
        run: |
          echo "success"
