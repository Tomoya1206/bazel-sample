---
name: codeQL-task
on:
  push:
    branches:
      - "develop"
      - "main"

jobs:
  codeql-job:
    name: codeql-job
    runson: ubuntu-latest
    steps:
      - name: Run codeQL-task
        run: |
          echo "success"
